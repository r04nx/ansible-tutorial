<!DOCTYPE html>
<html>
<head>
    <title>Test Page - {{ nginx_server_name }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: {{ nginx_color }};
            color: white;
        }
        .container {
            background-color: rgba(0, 0, 0, 0.6);
            border-radius: 10px;
            padding: 20px;
            max-width: 800px;
            margin: 0 auto;
        }
        h1 {
            border-bottom: 1px solid #ccc;
            padding-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{ nginx_welcome_message }}</h1>
        <p>This page is dynamically generated using Ansible templates.</p>
        <p>Server: {{ inventory_hostname }}</p>
        <p>Date Generated: {{ ansible_date_time.date }}</p>
    </div>
</body>
</html>
